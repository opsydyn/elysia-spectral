extends:
  - spectral:oas

rules:
  sample-autodiscover-yaml-summary:
    description: Require summaries through autodiscovered YAML ruleset.
    severity: warn
    given: $.paths[*][get,put,post,delete,options,head,patch,trace]
    then:
      field: summary
      function: truthy
